# Kotlin-vs-Java-Benchmarks
JMH benchmark suite comparing Java and Kotlin runtime performance and memory usage on the JVM - accompanying an empirical study on execution time, heap allocation, and GC behavior.
